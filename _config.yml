# Site Build Settings

# Jekyll-paginate-v2 configuration
#  pagination:
#   enabled: true
#   per_page: 5
#   permalink: '/page/:num/'
#   title: ':title - page :num of :max'
#   limit: 0
#   sort_field: 'date'
#   sort_reverse: true


# jekyll-paginate configuration
paginate:            5

#pagination: 
#  enabled: true
#  collection: 'posts'
#  per_page: 10
#  permalink: '/page/:num/' # Pages are index.html inside this folder (default)
#  title: ':title - page :num'
#  limit: 0
#  sort_field: 'date'
#  sort_reverse: true
#  category: 'posts'
#  tag: ''
#  trail: 
#    before: 2
#    after: 2
#  extension: html
#  indexpage: 'index'


# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           none
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           'statsandstuff'
description:	       'a blog on statistics and machine learning'
author:              'Scott Roy'

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
